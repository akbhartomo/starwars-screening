import React from 'react';

export const TitleRelatedMovie = ({ title }) => {
  return (
    <div>
      <h2 className="sub-title">
        {title}
      </h2>
    </div>
  );
};

export default TitleRelatedMovie;
